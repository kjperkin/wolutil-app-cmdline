apply plugin: "application"
applicationName = "wolutil"
mainClassName = "com.lightbulbz.wolutil.WolUtil"
sourceCompatibility = 1.7

dependencies {
    compile project(':libraries:wolutil-lib-common')
    runtime project(':libraries:wolutil-lib-common')
}
